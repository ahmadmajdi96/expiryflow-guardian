# CORTA WMS — ExpirySmart Warehouse Management System

A warehouse management system with FEFO-based inventory allocation, batch tracking, QC inspection, and expiry management.

## Running with Docker

```bash
docker compose up --build
```

The app will be available at `http://localhost:6001`.
